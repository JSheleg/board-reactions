import { useState } from 'react';
import { Link } from 'react-router-dom';
import { useQuery, useMutation } from '@apollo/client';

import { UPDATE_PASSWORD } from '../utils/mutations';

const ForgotPW = () => {
    return(
        <div>
            This is the ForgotPW page
        </div>
    )
}

export default ForgotPW;